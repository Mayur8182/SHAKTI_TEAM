{
  "name": "shakti-team",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "python -m gunicorn --worker-class eventlet -w 1 'fire.app:app'",
    "build": "pip install --upgrade pip && pip install -r requirements.txt && npm install",
    "dev": "python fire/app.py"
  },
  "engines": {
    "node": ">=14.0.0",
    "python": "3.9.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.0",
    "dotenv": "^10.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
